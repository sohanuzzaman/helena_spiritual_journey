---
interface Props {
  quote: string;
}

const { quote } = Astro.props;
---

<blockquote class="font-heading italic font-normal text-gold-glow border-l-2 border-l-gold-ancient pl-6 max-w-[680px] mx-auto my-12 leading-snug" style="font-size: clamp(1.5rem, 3vw, 2rem);">
  {quote}
</blockquote>
