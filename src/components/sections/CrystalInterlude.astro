---
// Full-bleed cinematic image break — Helena with amethyst crystal at the ocean
// Decorative only: no semantic content, aria-hidden
---

<div class="crystal-interlude reveal" aria-hidden="true">
  <img
    src="/helena-white-dress-amethyst-crystal-ocean-beach.jpg"
    alt=""
    loading="lazy"
    decoding="async"
    class="absolute inset-0 w-full h-full object-cover object-[center_35%]"
  />

  <!-- Purple and Gold radial vignette from edges -->
  <div
    class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_transparent_20%,_rgba(106,27,154,0.15)_60%,_rgba(18,3,34,0.85)_100%)]"
  >
  </div>

  <!-- Ambient magical glow orb -->
  <div
    class="glow-orb w-[400px] h-[400px] bg-gold-solar/15 top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2"
    aria-hidden="true"
  >
  </div>

  <!-- Top edge blend into previous section (abyss) -->
  <div
    class="absolute inset-x-0 top-[-5px] h-96"
    style="background: linear-gradient(to bottom, var(--color-abyss) 0%, rgba(11,0,20,0.95) 15%, rgba(11,0,20,0.85) 30%, rgba(11,0,20,0.65) 50%, rgba(11,0,20,0.3) 75%, rgba(11,0,20,0.05) 90%, transparent 100%);"
  >
  </div>

  <!-- Bottom edge blend into next section (depth) -->
  <div
    class="absolute inset-x-0 bottom-[-5px] h-[500px]"
    style="background: linear-gradient(to top, var(--color-depth) 0%, var(--color-depth) 5%, rgba(26,10,46,0.95) 20%, rgba(26,10,46,0.85) 40%, rgba(26,10,46,0.6) 60%, rgba(26,10,46,0.25) 80%, rgba(26,10,46,0.05) 95%, transparent 100%);"
  >
  </div>
</div>

<style>
  .crystal-interlude {
    position: relative;
    width: 100%;
    height: 90vh;
    min-height: 600px;
    overflow: hidden;
  }
</style>
